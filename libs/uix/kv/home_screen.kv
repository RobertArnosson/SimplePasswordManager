<HomeScreen>

    MDBoxLayout:
        orientation: "vertical"
        padding: 0, 0, 0, dp(15)

        ScrollView:
            MDList:
                id: item_list

        MDFloatingActionButton:
            icon: "plus"
            pos_hint: {"right": 0.95}
            on_release: root.goto('add')
